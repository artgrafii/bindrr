# bindrr web

Laravel app for personal spaces. Preview root for the bindrr monorepo.

See the repository root [README](../../README.md) for the product, S3 environment, and agent API.

```bash
composer install
npm install
npm run build
php artisan test
```
